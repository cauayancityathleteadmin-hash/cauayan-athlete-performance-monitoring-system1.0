{
  "buildCommand": "npx prisma db push --force-reset --skip-generate && SEED_TEST_DATA=1 node prisma/seed.js && next build",
  "installCommand": "npm install",
  "crons": [
    {
      "path": "/api/system/backup/auto",
      "schedule": "0 3 1 * *"
    }
  ]
}
