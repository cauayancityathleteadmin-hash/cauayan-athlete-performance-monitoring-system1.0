{
  "buildCommand": "prisma migrate deploy || echo \"[migrate] skipped\"; prisma db push || echo \"[db-push] skipped\"; next build",
  "installCommand": "npm install",
  "crons": [
    {
      "path": "/api/system/backup/auto",
      "schedule": "0 3 1 * *"
    }
  ]
}
