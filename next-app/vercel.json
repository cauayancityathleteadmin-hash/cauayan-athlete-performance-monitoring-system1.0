{
  "buildCommand": "prisma migrate deploy || echo \"[migrate] skipped\"; next build",
  "installCommand": "npm install",
  "crons": [
    {
      "path": "/api/system/backup/auto",
      "schedule": "0 3 1 * *"
    }
  ]
}
